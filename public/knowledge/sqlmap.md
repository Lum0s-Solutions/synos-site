# sqlmap

sqlmap is an open-source penetration-testing tool that automates the detection and exploitation of SQL injection vulnerabilities. It supports many database backends and injection techniques (boolean- and time-based blind, error-based, UNION query, and stacked queries) and can enumerate databases, dump data, read and write files, and gain OS-level command execution. It is the standard automated SQL injection tool.

> Part of [[cybersecurity-MOC]]

## Mentioned in
```dataview
LIST
FROM [[]]
SORT file.name
```

## 🎴 Review
<!-- Spaced Repetition cards -->
#flashcards/security-plus

What does sqlmap do?::Automates the detection and exploitation of SQL injection vulnerabilities across many database backends.

Which SQL injection techniques does sqlmap support?::Boolean- and time-based blind, error-based, UNION query, and stacked queries.

Beyond dumping data, what else can sqlmap achieve?::Enumerate databases, read and write files, and gain OS-level command execution.
