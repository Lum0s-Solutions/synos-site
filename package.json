{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "gen:play-test": "node scripts/gen-play-test.mjs",
    "prebuild": "node scripts/gen-play-test.mjs",
    "build": "astro build",
    "postbuild": "npx pagefind --site dist --output dist/pagefind",
    "preview": "astro preview",
    "test": "node --test tests/",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/starlight": "^0.38.3",
    "@pagefind/default-ui": "^1.5.2",
    "astro": "^6.0.1",
    "sharp": "^0.34.2"
  }
}
