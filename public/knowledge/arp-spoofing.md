# ARP Spoofing

ARP spoofing (ARP poisoning) is a man-in-the-middle attack in which an attacker sends forged ARP messages on a local network to associate their MAC address with another host's IP, redirecting that host's traffic through the attacker's machine. It enables interception, modification, or denial of traffic between victims on the same LAN. Defenses include dynamic ARP inspection, static ARP entries, and end-to-end encryption.

> Part of cybersecurity MOC

## Mentioned in
```dataview
LIST
FROM
SORT file.name
```

## 🎴 Review
<!-- Spaced Repetition cards -->
#flashcards/security-plus

What is ARP spoofing (ARP poisoning)?::A man-in-the-middle attack where an attacker sends forged ARP messages to associate their MAC address with another host's IP, redirecting that host's traffic through the attacker.

What can an attacker do once ARP spoofing succeeds?::Intercept, modify, or deny traffic between victims on the same LAN.

What network scope does ARP spoofing operate in?::The local network (LAN); it targets hosts on the same local segment.

What defenses mitigate ARP spoofing?::Dynamic ARP inspection, static ARP entries, and end-to-end encryption.
