# Hydra

Hydra (THC-Hydra) is a fast, parallelized network login brute-forcer that performs online password-guessing attacks against many protocols, including SSH, FTP, HTTP(S), RDP, SMB, and databases. Testers use it to assess credential strength and to detect weak or default logins on live services. Unlike offline crackers, it attacks running network services directly.

> Part of cybersecurity MOC

## Mentioned in
```dataview
LIST
FROM
SORT file.name
```

## 🎴 Review
<!-- Spaced Repetition cards -->
#flashcards/security-plus

What is Hydra (THC-Hydra)?::A fast, parallelized network login brute-forcer that performs online password-guessing attacks against many protocols.

Which protocols can Hydra attack?::SSH, FTP, HTTP(S), RDP, SMB, and databases.

How does Hydra differ from offline crackers?::It attacks running network services directly (online), rather than cracking captured hashes offline.
