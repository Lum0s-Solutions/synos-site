---
title: synos-hardware-accel — Syn_OS hardware acceleration daemon
description: synos-hardware-accel — Syn_OS hardware acceleration daemon
---

# synos-hardware-accel — Syn_OS hardware acceleration daemon

**Classification:** PUBLIC
**Crate:** `fruit/crates/synos-hardware-accel/`
**Milestone:** v1+
**License:** MIT OR Apache-2.0

## What It Is

`synos-hardware-accel` Syn_OS hardware acceleration daemon.

## Architecture

### Modules

| Module | Purpose |
|--------|---------|
| `HardwareConfig` | `HardwareConfig` module |
| `HardwareAccelerator` | `HardwareAccelerator` module |
| `new` | `new` module |
| `HardwareStatus` | `HardwareStatus` module |

### How It's Wired

1. **Internal** — self-contained crate

## Future Ideas

1. Expand module coverage and integration points
