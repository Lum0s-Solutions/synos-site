# tcpdump

tcpdump is a command-line packet analyzer that captures and displays network traffic passing through an interface using the libpcap library. Analysts pair it with BPF filter expressions to inspect protocols, troubleshoot connectivity, and record traffic to pcap files for later analysis in tools like Wireshark. It is a staple utility for network forensics and low-level protocol debugging.

> Part of cybersecurity MOC

## Mentioned in
```dataview
LIST
FROM
SORT file.name
```

## 🎴 Review
<!-- Spaced Repetition cards -->
#flashcards/security-plus

What is tcpdump?::A command-line packet analyzer that captures and displays network traffic on an interface using the libpcap library.

What are tcpdump BPF filter expressions used for?::To inspect specific protocols, troubleshoot connectivity, and target the traffic being captured.

What file format does tcpdump record to for later analysis?::pcap files, which can be analyzed later in tools like Wireshark.
